# 扫地机 Executor 迭代四方案（第二版评审稿）

## 1. 迭代目标

迭代四在迭代一至三的基础上增加“经过指定坐标时执行一次清扫”的能力。

本次评审已确定：

1. 支持多个清扫点；
2. 清扫点不能是扫地机的初始化位置；
3. 一次移动多格时必须逐格移动、逐格检查，沿途经过清扫点也要清扫；
4. 首次经过清扫点时调用 `void clean(int x, int y)`；
5. 同一个清扫点只调用一次 `clean()`；
6. 坐标同时是危险点和清扫点时，只告警，不清扫；
7. `clean()` 没有返回值，只打印清扫信息，调用后视为清扫完成；
8. 重新执行 `config_init_executor()` 时，重置所有清扫完成状态。

## 2. 本迭代范围

本迭代实现：

- Config 保存、校验并下发多个清扫点；
- Executor 保存多个清扫点及每个点的完成状态；
- 前进和后退按单位距离逐格移动；
- 每移动一格就检查危险点和清扫点；
- 危险点判断优先，危险点禁止清扫；
- 每个清扫点最多触发一次 Clean；
- Clean 打印清扫坐标，不返回执行结果；
- 重新初始化时重置清扫状态；
- 增加迭代四 GoogleTest，并回归迭代一至三。

本迭代暂不实现：

- 清扫成功或失败反馈；
- 清扫失败重试；
- 清扫持续时间；
- 清扫状态持久化；
- Wifi 上报。

## 3. 组件职责

```text
Config ----配置多个清扫点----> Executor ----触发----> Clean
                                  |
Controller ----动作命令----------+
                                  |
                                  +---------> Alert
```

### 3.1 Config

Config 负责：

- 保存多个清扫点；
- 拒绝与初始化位置相同的清扫点；
- 拒绝重复清扫点；
- 限制清扫点数量；
- 初始化 Executor 时批量下发清扫点。

Config 不负责判断是否到达清扫点，也不保存运行期间的清扫完成状态。

### 3.2 Executor

Executor 负责：

- 保存清扫点及每个点的完成状态；
- 前进、后退时逐格更新坐标；
- 每一步移动后统一执行危险点和清扫点检查；
- 命中危险点时触发 Alert 并跳过 Clean；
- 首次命中普通清扫点时调用 Clean；
- 保证已完成的清扫点不重复清扫。

### 3.3 Clean

Clean 只负责接收坐标并打印清扫信息：

```c
void clean(int x, int y);
```

建议输出：

```text
clean: x=2, y=3
```

该接口没有返回值。Executor 调用后直接把对应清扫点标记为已完成，不处理成功、失败或重试。

## 4. 数据模型

最多支持的清扫点数量使用常量控制：

```c
#define MAX_CLEAN_POINTS 16
```

Executor 中的清扫点需要记录运行状态：

```c
typedef struct {
    int x;
    int y;
    int cleaned;
} CleanPoint;
```

Executor 增加：

```c
CleanPoint clean_points[MAX_CLEAN_POINTS];
int clean_count;
CleanHandler clean_handler;
```

Config 只保存配置，不保存 `cleaned`：

```c
typedef struct {
    int x;
    int y;
} CleanPointConfig;

typedef struct {
    int x;
    int y;
    Heading heading;

    DangerPoint danger_points[MAX_DANGER_POINTS];
    int danger_count;

    CleanPointConfig clean_points[MAX_CLEAN_POINTS];
    int clean_count;
} Config;
```

`cleaned` 是 Executor 的运行状态。每次重新初始化 Executor 时，所有清扫点重新下发，并将 `cleaned` 设置为 `0`。

## 5. 接口设计

### 5.1 Clean 组件

新增 `include/clean.h`：

```c
#ifndef CLEAN_H
#define CLEAN_H

void clean(int x, int y);

#endif
```

新增 `src/clean.c`：

```c
#include "clean.h"

#include <stdio.h>

void clean(int x, int y)
{
    printf("clean: x=%d, y=%d\n", x, y);
}
```

### 5.2 Config 接口

```c
int config_add_clean_point(Config *config, int x, int y);
```

返回值：

- `1`：添加成功；
- `0`：等于初始化位置、重复或达到容量上限。

### 5.3 Executor 接口

```c
int executor_add_clean_point(Executor *executor, int x, int y);
```

Executor 再次校验当前位置、重复点和容量，避免其他调用方绕过 Config。

测试 Clean 调用次数和参数时，沿用迭代三的处理器注入方式：

```c
typedef void (*CleanHandler)(int x, int y);

void executor_set_clean_handler(Executor *executor, CleanHandler handler);
```

正式运行默认使用：

```c
executor->clean_handler = clean;
```

GoogleTest 使用 Fixture，在每个测试前重置记录状态，并注入 `RecordClean`。

## 6. 多清扫点检查

Executor 遍历所有清扫点，只处理当前位置对应且尚未完成的点：

```c
static void executor_check_clean(Executor *executor)
{
    int i;

    for (i = 0; i < executor->clean_count; ++i) {
        CleanPoint *point = &executor->clean_points[i];

        if (!point->cleaned &&
            executor->x == point->x &&
            executor->y == point->y) {
            executor->clean_handler(executor->x, executor->y);
            point->cleaned = 1;
            return;
        }
    }
}
```

清扫完成状态保留在对应数组元素中，所以离开后再次经过相同点不会重复调用 Clean，其他尚未完成的清扫点仍可正常触发。

## 7. 危险点优先规则

现有危险点检查函数需要改为返回是否命中危险点：

```c
static int executor_check_danger(const Executor *executor)
{
    int i;

    for (i = 0; i < executor->danger_count; ++i) {
        if (executor->x == executor->danger_points[i].x &&
            executor->y == executor->danger_points[i].y) {
            executor->alert_handler(IN_DANGEROUS,
                                    executor->x,
                                    executor->y);
            return 1;
        }
    }
    return 0;
}
```

每一步移动后按以下顺序处理：

```c
static void executor_after_step(Executor *executor)
{
    if (executor_check_danger(executor)) {
        return;
    }

    executor_check_clean(executor);
}
```

如果一个坐标同时被配置为危险点和清扫点：

1. 每次到达都会调用 Alert；
2. 本次不会调用 Clean；
3. 该清扫点保持未完成状态；
4. 以后再次到达仍然只告警、不清扫。

## 8. 逐格移动设计

当前前进、后退一次直接修改多个单位。迭代四改为循环逐格移动。

前进：

```c
void executor_forward_mile(Executor *executor, int distance)
{
    int step;

    for (step = 0; step < distance; ++step) {
        executor_move_one_step_forward(executor);
        executor_after_step(executor);
    }
}
```

后退：

```c
void executor_backward_mile(Executor *executor, int distance)
{
    int step;

    for (step = 0; step < distance; ++step) {
        executor_move_one_step_backward(executor);
        executor_after_step(executor);
    }
}
```

例如：

```text
初始位置：(0,0)，朝北
清扫点：(0,1)、(0,2)
命令：前进3格
```

实际执行：

```text
(0,0) → (0,1)：清扫第一个点
      → (0,2)：清扫第二个点
      → (0,3)：无清扫
```

因此一次命令可以触发多个沿途清扫点。

本方案规定 `distance` 为非负数。负数距离不属于迭代四范围。

## 9. 转向命令的处理

转向不会产生新的位置，因此不触发 Clean 检查。

为了保持迭代三“在危险点反复执行命令会重复告警”的要求，转向命令仍调用危险点检查，但不调用清扫点检查：

```c
void executor_turn_right(Executor *executor)
{
    executor->heading = (Heading)((executor->heading + 1) % 4);
    executor_check_danger(executor);
}
```

## 10. 重新初始化规则

`config_init_executor()` 的处理顺序：

1. 调用 `executor_init()` 重置坐标、朝向、危险点数量和清扫点数量；
2. 重新下发全部危险点；
3. 重新下发全部清扫点；
4. 每个清扫点的 `cleaned` 初始化为 `0`；
5. Alert 和 Clean 处理器恢复为默认真实函数。

因此重新初始化相当于开始一次新的运行。之前已完成的清扫点可以再次被清扫。

## 11. 测试方案

新增 `test/test_iteration4.cpp`，使用 GoogleTest Fixture。

Fixture 的 `SetUp()` 每次重置：

- Clean 调用次数；
- 最后一次 Clean 的坐标；
- Alert 调用次数；
- 最后一次 Alert 的类型与坐标。

### 11.1 Config 测试

| 用例 | 操作 | 预期结果 |
|---|---|---|
| 清扫点等于初始位置 | 添加 `(0,0)` | 返回失败，数量仍为0 |
| 添加多个合法点 | 添加 `(0,1)`、`(0,2)` | 均成功，数量为2 |
| 添加重复点 | 再添加 `(0,1)` | 返回失败，数量仍为2 |
| 超出容量 | 添加超过 `MAX_CLEAN_POINTS` | 超出部分返回失败 |
| Config 下发 | 初始化 Executor | 所有点存在且均未清扫 |

### 11.2 逐格清扫测试

| 用例 | 操作 | 预期结果 |
|---|---|---|
| 尚未到达 | 清扫点 `(0,3)`，前进2格 | Clean 调用0次 |
| 沿途一个清扫点 | 清扫点 `(0,2)`，前进3格 | 在 `(0,2)` 调用1次 |
| 沿途多个清扫点 | 清扫点 `(0,1)`、`(0,2)`，前进3格 | 按顺序调用2次 |
| 后退经过清扫点 | 后退路径经过指定点 | 到达时调用1次 |
| 离开后再次经过 | 往返经过同一点 | 总调用次数仍为1 |
| 未配置清扫点 | 执行多个移动命令 | Clean 调用0次 |

### 11.3 Alert 与 Clean 组合测试

| 用例 | 预期结果 |
|---|---|
| 危险点与清扫点不同 | 分别在对应坐标触发 |
| 同一坐标既危险又需清扫 | Alert 调用，Clean 不调用 |
| 重复经过重合点 | 每次告警，始终不清扫 |
| 一条路径先清扫后危险 | 先 Clean，后 Alert，最终位置正确 |

### 11.4 重置和回归测试

| 用例 | 预期结果 |
|---|---|
| 清扫后重新初始化 | 清扫状态重置，再次经过会再次清扫 |
| 迭代一回归 | 初始化、左右转和查询通过 |
| 迭代二回归 | 前进、后退和掉头通过 |
| 迭代三回归 | 多危险点和重复告警通过 |

## 12. 文件变更计划

预计新增：

```text
include/clean.h
src/clean.c
test/test_iteration4.cpp
```

预计修改：

```text
include/config.h
include/executor.h
src/config.c
src/executor.c
CMakeLists.txt
```

Controller 不增加清扫命令，因为清扫由移动路径自动触发。

## 13. 验收标准

- 支持最多 `MAX_CLEAN_POINTS` 个清扫点；
- Config 和 Executor 均拒绝初始位置、重复点和超容量添加；
- 前进和后退均逐格移动、逐格检查；
- 一次多格移动能触发沿途的一个或多个清扫点；
- 每个普通清扫点在一次初始化周期内只调用一次 Clean；
- 危险点优先，危险点与清扫点重合时始终不清扫；
- `clean()` 打印坐标且没有返回值；
- 重新初始化后清扫完成状态全部重置；
- 迭代一至四测试全部通过；
- C/C++ 编译无告警。
